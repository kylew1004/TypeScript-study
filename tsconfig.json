{
  "include": ["src"], // TS가 실행 될 경로
  "compilerOptions": {
    "outDir": "build", // JS가 실행 될 경로
    "target": "ES6", // 어떤 버전의 JS를 실행시킬지 정함
    "lib": [
      "ES6"
    ] /*어떤 API를 사용하고 어떤 환경에서 코드를 실행하는 지를 지정할 수 있습니다. 프로그램이 브라우저에서 실행되면 lib에 "DOM" 유형 정의를 할 수 있습니다.
        DOM: window, document 등 */,
    "strict": true,
    "esModuleInterop": true,
    "module": "CommonJS"
  }
}
